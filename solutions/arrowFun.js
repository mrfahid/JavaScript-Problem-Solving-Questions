// Write a JavaScript function using arrow syntax that takes two parameters and returns their sum.

const sumNum = (num1, num2) => {
  return num1 + num2
}

let result = sumNum(220, 10)
console.log(result);